[package]
name = "hk_generator"
version = "0.1.0"
edition = "2021"

[dependencies]
# Обертка над LLVM
inkwell = { version = "0.4.0", features = ["llvm14-0"] }
# Для работы с C-интерфейсами CAmkES
libc = "0.2"